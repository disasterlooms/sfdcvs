<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Focus</fullName>
    <active>false</active>
    <description>Disallows account name changes on focus accounts because it will affect reporting.</description>
    <errorConditionFormula>ischanged(Name) &amp;&amp; Focus_Account__c == true</errorConditionFormula>
    <errorMessage>You cannot change a focus account name</errorMessage>
</ValidationRule>
