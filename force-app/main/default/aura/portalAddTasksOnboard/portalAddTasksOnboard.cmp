<aura:component access="global" implements="lightning:availableForFlowScreens" controller="portalOnboardingAssignTask">
        <aura:handler name="init" value="{!this}" action="{!c.addTaks}" />
   <!-- Determine which actions are available -->
   <aura:attribute name="con" type="String" />  
</aura:component>